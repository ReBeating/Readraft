# 安全政策

## 支持范围

安全修复优先提供给最新正式版本和当前 `main`。较旧版本可能需要先升级
才能获得修复。

## 私下报告漏洞

请使用 GitHub 仓库 **Security → Report a vulnerability** 创建私密
安全报告。不要在公开 Issue、Discussion、Pull Request 或截图中披露：

- API Key、Cookie、加密密钥或密码；
- 真实作品、聊天、数据库或备份；
- 可直接利用但尚未修复的攻击步骤。

报告中请提供受影响版本或提交、部署方式、最小复现步骤、预期影响和已经
做过的缓解。日志和请求内容必须先脱敏。维护者会尽力确认、复现和修复，
但当前项目不承诺固定响应时限。

如果凭据已经进入公开记录，请先在对应服务商处撤销或轮换；从 Git 历史
删除字符串不能让已泄露凭据重新安全。

## 安全边界

- Readraft 是自托管应用，部署者拥有主机、数据库和加密密钥的最终控制权。
- 个人模型凭据经过认证加密后保存在 SQLite，但拥有数据库和安装级加密
  密钥的管理员仍可解密；这不是端到端加密。
- 生产环境必须使用 HTTPS、安全 Cookie、随机的 `APP_SECRET_KEY` 和独立
  稳定的 `APP_CREDENTIAL_ENCRYPTION_KEY`。
- Ollama 和 OpenAI 兼容接口可访问用户配置的地址。只有受信任账号才应
  获得该能力，生产环境不应随意允许本机或内网目标。
- 完整备份包含账号、正文和加密后的模型凭据，应按敏感数据保存。

部署加固和升级方式见 [docs/deployment.md](docs/deployment.md)。
