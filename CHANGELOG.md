# 更新记录

本项目使用语义化版本。公开版本的重要变化记录在这里。

## 0.1.7 — 2026-08-05

- 版本模型收敛为每章唯一 `main HEAD`、不可变历史和整书 Tag，删除工作稿/正史晋升、
  逐场景正文、场景组装和硬审核执行链；
- Agent 改为 Provider 原生工具回合，加入受控虚拟工作区、精确 edit/patch、章节
  create/compose/series 和只读专项 task，并取消默认工具总预算与模型回合配额；
- 对话服务、导入、分析、写作上下文和主要 Web 路由继续按应用层与 repository 边界拆分；
- 新增 DeepSeek、OpenCode Go、自定义 OpenAI、OpenAI、Gemini 与 Ollama 的中性协议和
  能力注册，并完善流式输出、结构化响应、推理策略和模型路由；
- TXT/Markdown 导入增加章节边界置信度预览，以及人工改名、合并和按光标拆分；
- 长篇上下文增加中文稀疏融合检索、对话历史资源和可重复 Recall@5/MRR 回归基准；
- 正文生成统一经过纯文本 compose 管线和集中式小说写作契约，连续多章任务可以断点恢复；
- 参考小说分析升级为结构、事实、叙事、文风和技法五层，并加入证据化章节文风判断；
- 新增全书文风画像，汇总句段节奏、对话占比、跨章覆盖率、抽象执行规则和原创性边界；
- 将分析 schema、度量、提示词、聚合与执行状态机拆成独立模块，Worker 不再承载整条
  章节分析流程；
- Agent 可以按需读取参考书全书画像；未明确要求参考文风时不会自动把描述性分析当成
  创作规则；
- 技法卡保存只校验实际消费的技法层，不再绑定整份章节分析 JSON 的旧结构。

## 0.1.5 — 2026-07-28

- 取消聊天输入框遗留的 8,000 字符限制；
- 服务端仅保留 100,000 字符安全上限，支持在讨论中粘贴长篇大纲和章节资料。

## 0.1.4 — 2026-07-28

- 增加 Low、Standard、Max 三档模型策略，并分别配置快速模型与高质量模型；
- 对话改用 Provider 原生 SSE 流式输出，修复发送瞬间闪回浅色主题的问题；
- 默认接入无需用户凭据的 Exa MCP 联网搜索，并保留账号级联网开关；
- Agent 只在需要近期或外部资料时搜索，单轮最多搜索两次；
- 模型设置弹窗增加模型策略与联网页面，对话框左下角可以切换本轮强度。

## 0.1.3 — 2026-07-27

- 将 AI 正文生成与正式版本提升合并为同一数据库事务；
- 修复任务偶尔先显示完成、正文和分析状态随后才同步的竞态。

## 0.1.2 — 2026-07-27

- 禁止生产安装过程把 pip 缓存写进部署 Git 工作树；
- 忽略已有的部署用户缓存目录，避免安全更新被误判为脏工作树。

## 0.1.1 — 2026-07-27

- 生产服务器可以只跟踪带注释的稳定语义版本 Tag，不必追踪 `main`；
- 增加互斥锁、更新前健康检查、隔离依赖环境和完整备份校验；
- 新版本启动失败时自动恢复旧代码、旧依赖和更新前数据；
- 增加默认每日检查、随机延迟执行且需要管理员主动启用的 systemd Timer。

## 0.1.0 — 2026-07-27

Readraft 首个公开 MVP：

- 统一作品库，可在同一作品下管理可写 `main` 与不可变 Tag；
- 支持 TXT/Markdown 导入、阅读分析、原创、续写、改写和 AI 对话；
- 支持 DeepSeek、OpenAI、Google Gemini、Ollama 与 OpenAI 兼容接口；
- 作品资料、规划、正文版本、正史记忆、审校与对话持久化；
- `.readraft.zip` 单作品迁移，以及带校验的实例级备份与恢复；
- SQLite 向前迁移、单进程持久任务队列和低内存 systemd 部署；
- Apache License 2.0、GitHub Actions、依赖审计和密钥扫描。
