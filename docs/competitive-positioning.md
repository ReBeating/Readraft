# novelAI：竞争定位与优势边界

> 文档状态：产品基线
>
> 更新日期：2026-07-26
>
> 名称说明：`novelAI` 只是当前开发工作名。市场上已经存在
> `NovelAI` 产品，公开发布前必须另行处理商标、域名和产品混淆问题。
> 本文用“本项目”指当前代码，用“NovelAI（现有产品）”指对方。

## 1. 一句话定位

> **本项目不是一个更会续写的聊天框，而是一套让长篇故事可规划、
> 可确认、可追溯、可恢复的创作系统。**

更准确的品类是：

> **作者控制的长篇叙事工作台。**

它把长篇创作视为持续演化、需要治理的故事状态，而不只是越来越长的
一段文本：

```text
作者意图
  → AI 候选
  → 作者确认
  → 规划 / 正文版本
  → 正史
  → Story Delta
  → 确定性状态重放
  → 下一章受约束的创作上下文
```

核心边界是：**模型可以提出、分析和起草，但不能把自己的输出直接
宣布为作品事实。**

## 2. 公平对比

| 代表产品 | 对方当前擅长什么 | 本项目的实现差异 | 对方明显更强的地方 |
|---|---|---|---|
| **Novelcrafter** | Codex、规划视图、写作、聊天、版本历史、协作和完整项目导出；可连接 300 多个模型及本地模型 | 不只把资料送进提示词，而是把“候选—确认—正史—状态重放”设为服务端状态机；异步结果带冻结基线和指纹，过期结果不能覆盖新工作 | 模型和供应商覆盖、协作、导入导出、交互成熟度与用户生态 |
| **Sudowrite** | Story Bible 从构思、梗概、人物、世界和提纲推进到场景与正文；另有 Canvas、插件、系列共享和移动端 | 进一步区分“未来规划”和“已经发生的正史”；正文确认后生成 Story Delta，再确定性重放人物知情、关系、伏笔、物品和剧情线状态 | 灵感与散文辅助、低学习成本、视觉规划、插件、移动端与系列体验 |
| **NovelAI（现有产品）** | 专用生成体验、模型和采样控制、Memory、Author's Note、Lorebook 条件注入、上下文查看器、脚本扩展与可移植故事导出 | 重点不在 token 级生成控制，而在全书方案、滚动结构、任务卡、场景执行、硬审计、正史确认和连续性回放组成的受控生命周期 | 模型与采样控制、流式生成、Lorebook 条件系统、脚本生态和成熟导出 |
| **Scrivener** | 长文拆分、研究资料、软木板、Outliner 和 Word/PDF/Final Draft 等成熟编译导出 | 让确认计划、人物知情、因果、伏笔和正史状态直接参与生成前约束与生成后审计，而不只由作者手工维护资料 | 纯写作体验、项目组织、排版编译、跨平台桌面体验和长期稳定性 |
| **Plottr** | 可视化时间线、情节线、场景卡、Story Bible、模板、系列规划，以及 Word/Scrivener 导出；Pro 版支持云同步和协作 | 任务卡中的推进线、必做事件、伏笔和钩子必须落到具体场景；缺失覆盖时服务端阻止确认，后续正史变化还会触发影响检查 | 时间线可视化、拖拽规划、模板、系列视图、同步、离线与协作 |

这里最接近本项目的是 **Novelcrafter**。因此，“有资料库、能规划、
能聊天、能保存版本”都不能算本项目的独特优势；成熟产品已经具备这些
能力。

## 3. 当前真正成立的优势

### 3.1 作者确认不是界面提示，而是数据边界

设定、全书蓝图、剧情线、章节任务卡、正文候选、审计覆盖和
Story Delta 都有明确状态。未确认资料不能进入下游权威上下文，模型
输出也不能自行成为正史。

### 3.2 长篇故事可以确定性重放

正史正文不是简单追加到提示词。章节确认后，系统把变化投影为
Story Delta，并按顺序重放人物状态、关系、物品、知情边界、剧情线、
伏笔和故事时间。相同正史输入会得到相同状态，问题可以定位到具体
章节和证据。

### 3.3 异步 AI 结果有基线和保质期

全书方案、滚动结构、场景计划、因果建议和长期沙盘在发起时冻结当前
计划、正史与结构指纹。作者在等待期间继续修改作品后，旧结果会被
识别为过期，而不是静默覆盖较新的决定。

### 3.4 规划必须落到可执行场景

章节任务卡中的每条推进线、必做事件、伏笔和章末钩子，都必须映射到
具体场景引用。场景版本还要经过场景检查，全部就绪后才能安全组装成
整章候选。

### 3.5 因果建议必须带证据和决策门

跨章因果候选引用冻结的章节、事件和计划证据，并分别检查事实、人物
知情、时间、世界规则和连续性。长期影响先进入只读三分支沙盘，再由
作者逐章编辑、接受或拒绝，不会直接改写未来任务。

### 3.6 生成、审计与风格学习彼此分离

Scene Auditor、Hard Auditor 和 Style Auditor 处理不同问题。失败
文本仍保留为不可变候选，但不会悄悄成为正史。参考作品只沉淀为带
证据和原创性边界的抽象技法卡；作者自己的手工改稿也要经过证据定位
和确认后，才会成为后续写作偏好。

### 3.7 本地数据可检查、可迁移、可恢复

项目使用 SQLite 和本地正文文件，个人模型凭据由作者提供。完整备份
会把数据库路径改写为可迁移标记，逐文件校验数据库、正文、历史版本
和参考资料；恢复前还会自动保留当前数据的安全归档。

## 4. 还不能当作优势宣传的部分

- 当前只正式接入 DeepSeek，模型广度远低于 Novelcrafter 和
  NovelAI（现有产品）。
- 高级状态治理能力已经进入代码和测试，但尚未全部整合进统一工作台；
  新用户不一定能发现或理解。
- 自动化测试证明结构、权限和状态门禁，不证明真实模型输出质量；
  固定样稿的真实 DeepSeek 验收仍需完成。
- SQLite、单进程和单后台 Worker 适合个人使用，不是多人规模优势。
- 五类语义预检能提供证据和决策门，不能证明自然语言事实或因果绝对
  正确。
- 当前没有成熟移动端、协作、可视化时间线、DOCX/EPUB 排版和发布
  生态。
- “本地优先”和“作者自带 Key”是隐私及成本取舍，不天然等于更好的
  创作体验。

因此，现阶段最可信的表达是：

> **其他工具通常优先帮助作者更快地产生和组织文本；本项目优先帮助
> 作者在作品越来越长之后，仍然知道什么已被确认、为什么成立、改动
> 会影响哪里，以及下一章应在什么约束下继续。**

## 5. 下一步如何证明优势

1. 用同一套 3–5 章样稿，分别验证规划、创作、审计、正史与记忆链，
   记录结构失败率、耗时、token、费用和人工评分。
2. 把任务卡、场景、审计、正史和记忆入口整合进统一工作台，观察首次
   使用者能否不猜 URL 完成闭环。
3. 设计“修改旧章后继续写”的对照任务，验证冻结基线、影响报告和
   状态重放是否真实减少连续性错误。
4. 持续执行完整备份和异地恢复演练，证明“可恢复”不是只有一个导出
   按钮。

## 6. 公开资料依据

- [Novelcrafter 产品能力](https://www.novelcrafter.com/)
- [Novelcrafter Revision History](https://docs.novelcrafter.com/en/articles/8677729-revision-history/)
- [Novelcrafter Full Project Export](https://feedback.novelcrafter.com/changelog/september-19-2024)
- [Sudowrite Story Bible](https://docs.sudowrite.com/using-sudowrite/1ow1qkGqof9rtcyGnrWUBS/what-is-story-bible/jmWepHcQdJetNrE991fjJC)
- [Sudowrite 功能目录](https://docs.sudowrite.com/using-sudowrite/1ow1qkGqof9rtcyGnrWUBS)
- [NovelAI Lorebook](https://docs.novelai.net/en/text/lorebook/)
- [NovelAI Story Settings](https://docs.novelai.net/en/text/editor/storysettings/)
- [NovelAI Context Settings](https://docs.novelai.net/en/text/editor/advancedsettings/)
- [NovelAI Scripting](https://docs.novelai.net/en/scripting/introduction/)
- [Scrivener Overview](https://www.literatureandlatte.com/scrivener/overview)
- [Plottr Features](https://plottr.com/features/)
