# 隐私说明

Readraft 是自托管软件。项目本身没有托管服务，也没有内置遥测、广告或
第三方分析 SDK。实际数据责任由运行实例的部署者承担。

## 本地保存的数据

实例会在配置的 `APP_DATA_DIR` 和 SQLite 数据库中保存：

- 账号、加盐密码哈希和会话所需标识；
- 导入原文、章节正文、不可变版本与作品资料；
- 阅读分析、笔记、故事记忆、审校结果、AI 对话和任务日志；
- 模型服务配置，以及经过认证加密的个人 API Key。

API Key 不会写入浏览器 Cookie，也不会完整回显。完整备份包含上述数据和
加密后的 API Key，但不包含 `.env` 与安装级加密密钥。

## 发送给模型服务的数据

执行 AI 任务时，Readraft 会把完成该任务所需的提示词、相关正文片段、
作品资料、记忆或分析发送给用户选择的模型服务商。具体接收方可能是
DeepSeek、OpenAI、Google Gemini、Ollama 或用户配置的 OpenAI 兼容
接口。模型服务商如何记录、训练或保留请求，取决于该服务商及账号条款，
不由 Readraft 控制。

自托管 Ollama 可以让模型请求留在部署者控制的网络内；自定义 Base URL
也可能把内容发送给未知第三方，部署者和用户应先确认目标可信。

## 部署者责任

面向他人提供 Readraft 实例时，部署者应：

- 明确告知用户实例运营者、存储位置、模型服务商和保留期限；
- 限制主机、数据库、备份和日志的访问权限；
- 提供账号及作品的导出、删除和安全事件通知方式；
- 根据适用法律和模型条款取得所需授权。

Apache License 2.0 仅适用于本仓库的代码和项目文档，不主张用户导入、
创作或生成内容的权利，也不替用户取得参考作品或模型输出的使用许可。
